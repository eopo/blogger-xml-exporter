# Golang CI Linter Configuration
# https://golangci-lint.run

version: "1.56"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - gofmt
    - goimports
    - gosimple
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - typecheck
    - unused
